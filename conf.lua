-- Configuration
function love.conf(t)
	t.title = "Skyfall" -- The title of the window the game is in (string)
	t.window.width = 1366        -- we want our game to be long and thin.
	t.window.height = 768

	-- For Windows debugging
	t.console = true
end